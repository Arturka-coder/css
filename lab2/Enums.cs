namespace CatAndMouse
{
    // Состояние игрока
    public enum State
    {
        Winner,
        Loser,
        Playing,
        NotInGame
    }

    // Состояние игры
    public enum GameState
    {
        Start,
        End
    }
}